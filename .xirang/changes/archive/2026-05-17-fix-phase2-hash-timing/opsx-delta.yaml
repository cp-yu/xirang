MODIFIED:
  capabilities:
    - id: cap.apply.verify-integration
      intent: apply 命令内置 Phase 1 一致性验证和 Phase 2 优化循环，由 subagent 执行验证判断和优化提案，主 agent 仅负责编码。Phase 2 optimization CLI 调用 MUST 在 patch 应用之前执行，确保 affectedFileHashes 采样 pre-patch 磁盘状态
