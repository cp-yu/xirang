name: spec-driven
version: 1
description: Default OpenSpec workflow - proposal → specs → design → tasks
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    definition:
      purpose: State why the change is needed and bound its capability impact.
      compilationRole: Compilation scaffolding for motivation and scope.
      content:
        includes:
          - Motivation, scope, capability impact, and affected surfaces.
        excludes:
          - Behavioral requirements, lowering decisions, implementation steps, and architecture relations.
      writePolicy: agent-authored
      validation:
        - openspec validate --change <name> --json
    instruction: |
      Create the proposal document that establishes WHY this change is needed.

      Sections:
      - **Why**: 1-2 sentences on the problem or opportunity. What problem does this solve? Why now?
      - **What Changes**: Bullet list of changes. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**.
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names (e.g., `user-auth`, `data-export`).
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Check `openspec/specs/` for existing spec names. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Research existing specs before filling this in.
      Each capability listed here will need a corresponding spec file.

      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      implementation details belong in design.md.

      This is the foundation - specs, design, and tasks all build on this.
    requires: []

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    definition:
      purpose: Define the durable observable behavior that the target program must exhibit.
      compilationRole: Behavior semantic source delta reconciled into formal Specs.
      content:
        includes:
          - Complete target-state requirements and scenarios using delta sections.
        excludes:
          - Implementation choices, change-log narration, architecture relations, and agent reasoning.
      writePolicy: agent-authored
      validation:
        - openspec validate --change <name> --artifacts specs --json
        - openspec scenario-labels <name> --write
    instruction: |
      Create specification files that define WHAT the system should do.

      Spec content boundary:
      - Allowed in specs: externally observable behavior, CLI/API inputs and outputs, errors, validation, permissions, persistence, compatibility, and testable constraints.
      - Forbidden in specs: implementation path or call graph, refactor rationale, rejected approaches, exploration/process notes, code organization, helper/library choices, and agent reasoning.
      - Route forbidden content to the right artifact: technical decisions and refactor rationale go in `design.md`; concrete implementation steps and checks go in `tasks.md`; motivation and scope go in `proposal.md`; architecture graph intent changes go in `opsx-delta.yaml`.
      - If "do not call/use the old path" is not externally observable, write it in `design.md`, not in a requirement. In specs, express the preserved or changed observable behavior instead.

      Requirement framing rules:
      - Requirement names MUST be durable capability names. FORBIDDEN: change-action names such as gap-closure, restore, unify, replace, or migrate.
      - One requirement covers one capability concern. Do not bundle multiple unrelated commands or features into one requirement - split them.
      - Behavior definitions MUST NOT reference change context such as "as promised", "the deprecated X", or "the original Y". State the behavior as it will be.
      - Litmus test: a reader two years later, unaware of this change, must understand the requirement standalone. When existing capability behavior changes, prefer MODIFIED on the existing requirement over ADDED of a change-named requirement.

      Before writing or modifying a requirement, classify each candidate statement:
      - Observable behavior or constraint -> keep in specs.
      - Why this approach, why not the old approach, or how to implement it -> move to design/tasks/proposal/opsx-delta.

      Create one spec file per capability listed in the proposal's Capabilities section.
      - New capabilities: use the exact kebab-case name from the proposal (specs/<capability>/spec.md).
      - Modified capabilities: use the existing spec folder name from openspec/specs/<capability>/ when creating the delta spec at specs/<capability>/spec.md.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - Do not author scenario operation labels before validation. Run `openspec scenario-labels "<change>" --write` after validation to generate deterministic change-local review metadata; sync/archive consume and clean existing labels but do not generate them.
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.
      - `### Requirement:` and `#### Scenario:` markers remain canonical.
      - New Requirement titles and new Scenario titles follow `proseLanguage` when configured.
      - Existing Requirement titles under `MODIFIED Requirements` remain exact for matching.

      MODIFIED requirements workflow:
      1. Locate the existing requirement in openspec/specs/<capability>/spec.md
      2. Copy the ENTIRE requirement block (from `### Requirement:` through all scenarios)
      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior
      4. Ensure header text matches exactly (whitespace-insensitive)

      Common pitfall: Using MODIFIED with partial content loses detail at archive time.
      If adding new concerns without changing existing behavior, use ADDED instead.

      Example:
      ```
      ## ADDED Requirements

      ### Requirement: User can export data
      The system SHALL allow users to export their data in CSV format.

      #### Scenario: Successful export
      - **WHEN** user clicks "Export" button
      - **THEN** system downloads a CSV file with all user data

      ## REMOVED Requirements

      ### Requirement: Legacy export
      **Reason**: Replaced by new export system
      **Migration**: Use new export endpoint at /api/v2/export
      ```

      Examples show structure only. When `proseLanguage` is configured, do not carry over ordinary English example prose into generated artifacts.
      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal

  - id: opsx-delta
    generates: opsx-delta.yaml
    description: Project OPSX delta describing architecture graph changes for this change
    template: opsx-delta.yaml
    definition:
      purpose: Define the minimal architecture reconciliation operations for the target OPSX model.
      compilationRole: Architecture semantic source delta reconciled into formal OPSX.
      content:
        includes:
          - Target-state node intents, statuses, and canonical semantic relations.
        excludes:
          - Code paths, symbols, imports, calls, change-log narration, and mechanical code evidence.
      writePolicy: agent-authored
      validation:
        - openspec validate --change <name> --artifacts opsx-delta --json
    instruction: |
      Create the OPSX delta document that captures project architecture graph changes for this change.

      Use `openspec/project.opsx.yaml` as current-system context when it exists.

      Output requirements:
      - The file MUST be valid YAML
      - Use `ADDED`, `MODIFIED`, and `REMOVED` as YAML object keys, not Markdown headings
      - Delta nodes contain only `id`, `type`, `intent`, and optional `status`
      - Do NOT include `code_refs` or `spec_refs` in delta nodes
      - Use only Registry relation types: belongs_to, invokes, consumes, precedes, constrains, validates
      - If no precise type applies, omit the relation and record a review gap

      Minimal shape:
      ```yaml
      schema_version: 2
      ADDED:
        capabilities:
          - id: cap.example.feature
            type: capability
            intent: Describe the new capability
        relations:
          - from: cap.example.feature
            type: belongs_to
            to: dom.example

      MODIFIED:
        capabilities:
          - id: cap.example.existing
            intent: Updated intent text

      REMOVED:
        capabilities:
          - id: cap.example.legacy
      ```

      Keep the delta focused on architecture intent, not implementation details.
    requires:
      - specs

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    definition:
      purpose: Record lowering decisions the compiler must not guess.
      compilationRole: Compilation scaffolding for technical decisions and constraints.
      content:
        includes:
          - Implementation strategy, rationale, alternatives, risks, and migration decisions.
        excludes:
          - Behavioral requirements, architecture graph facts, task progress, and repeated motivation.
      writePolicy: agent-authored
      validation:
        - openspec validate --change <name> --json
    instruction: |
      Create the design document that explains HOW to implement the change.
      Use design.md for technical decisions, refactor rationale, rejected approaches, exploration decisions, implementation strategy, and why an old path should not be used. These belong here instead of specs unless they change externally observable behavior.

      When to include design.md (create only if any apply):
      - Cross-cutting change (multiple services/modules) or new architectural pattern
      - New external dependency or significant data model changes
      - Security, performance, or migration complexity
      - Ambiguity that benefits from technical decisions before coding

      Sections:
      - **Context**: Background, current state, constraints, stakeholders
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered for each decision.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Outstanding decisions or unknowns to resolve

      Focus on architecture and approach, not line-by-line implementation.
      Reference the proposal for motivation and specs for requirements.

      Good design docs explain the "why" behind technical decisions.
    requires:
      - proposal

  - id: tasks
    generates: tasks.md
    description: Coarse implementation tasks with verification checks
    template: tasks.md
    definition:
      purpose: Decompose compilation into executable, evidence-gated work units.
      compilationRole: Compilation scaffolding for implementation sequencing and verification evidence.
      content:
        includes:
          - Coarse tasks, owned files, requirements, checks, commands, and evidence anchors.
        excludes:
          - New product behavior, architecture source, duplicated design rationale, and untestable narration.
      writePolicy: agent-authored
      validation:
        - openspec validate --change <name> --json
    instruction: |
      Create a coarse-grained task list with executable Checks that the apply phase can run through strict evidence gates.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses
      `### Task N:` headings and nested checkboxes to track progress.

      Required structure:
      - Create one `### Task N: <name>` section per component or coherent work unit
      - Each task MUST include `**Goal**:`, `**Files**:`, `**Requirements**:`, and `#### Checks`
      - `Files` MUST use POSIX project-relative paths and label each path as `Create:`, `Modify:`, `Delete:`, or `Test:`
      - File-level deletions (including moves and merges that produce deletions) use `Delete:` declaration; generated-surface changes (such as skill regeneration directories) should be declared at directory granularity
      - `Requirements` MUST contain no more than 5 bullet items per task; split complex work into multiple tasks
      - `Checks` are executable verification items and MUST use checkbox IDs prefixed with `C`: `- [ ] C1 Verify ...`
      - Each check MUST include `Verifies:` or `Preserves:` before executable evidence fields
      - `Verifies:` identifies the change-local spec requirement and scenario(s); for deletion deliverables, use `Verifies: ... / REMOVED Requirement "<name>"` (no Scenario required)
      - `Preserves:` anchors behavior-equivalence checks to main spec paths (`openspec/specs/<capability>/spec.md` / Requirement "<name>" / Scenario "<name>"), naming old forms in `Expect:` and absence assertion in `Command:`
      - Each check MUST include at least one executable evidence field: `Command:`, `Evidence:`, or `Expect:`
      - When this change has local delta specs, `Verifies:` MUST use a change-local relative POSIX path from the change directory: `specs/<capability>/spec.md`
      - `Verifies:` MUST name the full Requirement title and one or more full clean Scenario names without scenario operation labels (for `#### Scenario: [ADDED] <title>`, use `Scenario "<title>"`; except for REMOVED Requirement variant)
      - `Verifies:` MUST NOT use `openspec/specs/...`, project-root-relative paths, archived/main spec paths, absolute paths, parent traversal, or backslash-separated paths
      - `Preserves:` path whitelist is independent: only `openspec/specs/<capability>/spec.md` is allowed; it does not relax `Verifies:` constraints

      Conversion rules:
      - Task titles, check names, requirements bullet prose, and Evidence:/Expect: descriptions follow `proseLanguage` when configured.
      - Structural labels such as `### Task N:`, `Goal`, `Files`, `Requirements`, `Checks`, `Verifies:`, `Command:`, `Evidence:`, and `Expect:` remain canonical.
      - Examples show structure only. When `proseLanguage` is configured, do not carry over ordinary English example prose into generated artifacts.
      - Generate one task from each major component in the design or Design Summary
      - Put technical stack constraints and boundary conditions under Requirements, not free-form prose
      - Validation work maps to checks for invalid-input behavior
      - Bug fixes map to a check that reproduces the failure and proves the regression fix
      - Refactors map to a behavior-equivalence check: use `Preserves:` field anchoring a main spec requirement with scenarios, `Expect:` naming the old form (old symbol, old path, or old duplication), and `Command:` with corresponding absence assertion
      - Deletion work maps to a check anchored by `Verifies: ... / REMOVED Requirement "<name>"` (no Scenario variant) with an absence assertion command
      - non-runtime text or non-runtime artifact changes do not require artificial failing tests, but still need at least one lightweight check with a non-empty `Verifies:` explaining how completion will be verified
      - config, schema, template, workflow template, and agent instruction template changes default to behavior Checks unless the task proves there is no runtime or generated-surface consumer
      - Absence assertions (such as grep empty assertions after deletion) are handled by the non-runtime text fast path: use `Command:` output as final evidence, do not require artificial red/green form
      - Existing Actions/Checks files remain valid for older changes, but new changes SHOULD use the Task/Goal/Files/Requirements/Checks format

      Example:
      ```
      ### Task 1: User registration API

      **Goal**: Implement email and password registration.

      **Files**:
      - Create: `src/routes/auth.ts`
      - Modify: `src/app.ts`
      - Test: `tests/auth.test.ts`

      **Requirements**:
      - Accept `POST /auth/register`
      - Validate email format
      - Hash passwords before storage

      #### Checks

      - [ ] C1 Verify registration endpoint
        - Verifies: `specs/auth/spec.md` / Requirement "User registration" / Scenario "Valid registration succeeds"
        - Command: `npm test`
        - Expect: registration test passes

      - [ ] C2 Verify invalid email rejection
        - Verifies: `specs/auth/spec.md` / Requirement "User registration" / Scenario "Invalid email is rejected"
        - Command: `npm test`
        - Expect: invalid email test passes
      ```

      Reference specs for what needs to be built, design for how to build it.
      Checks are executable verification work, not explanatory prose. The apply phase will execute each pending behavior/code Check through strict red/green TDD and mark checks complete only after red/green evidence or final non-runtime evidence passes.
    requires:
      - specs
      - design

apply:
  requires: [tasks]
  tracks: tasks.md
  instruction: |
    Read context files and execute each pending behavior/code Check through strict red/green TDD: add or update the targeted test first, run the Check command or equivalent targeted command and confirm the expected failure before implementation, make the minimal implementation, then rerun and confirm pass before marking nested Checks complete.
    For non-runtime text or artifact Checks, do not create artificial failing tests; use the declared Command, Evidence, or Expect fields as final evidence.
    Config, schema, template, workflow template, and agent instruction template Checks default to behavior Checks unless the task proves there is no runtime or generated-surface consumer.
    Pause if you hit blockers or need clarification.
